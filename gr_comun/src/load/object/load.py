class Load:
    def __init__(self,
                 demand_id: str = None,
                 demand_factor: float = None,
                 area: str = None,
                 ):
        self.demand_id = demand_id
        self.demand_factor = demand_factor
        self.area = area
